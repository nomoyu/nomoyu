package {{ .Context }}

import "time"

type {{ .ContextP }} struct {
	ID        uint64
	Name      string
	CreatedAt time.Time
}

func New{{ .ContextP }}(name string) (*{{ .ContextP }}, error) {
	return &{{ .ContextP }}{Name: name, CreatedAt: time.Now()}, nil
}
